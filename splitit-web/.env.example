# URL du backend
NEXT_PUBLIC_API_URL=https://splitit-9x32.onrender.com

# Clé publique VAPID pour les notifications push web
# Génère une paire de clés avec : npx web-push generate-vapid-keys
# La clé privée va dans le backend (process.env.VAPID_PRIVATE_KEY)
NEXT_PUBLIC_VAPID_PUBLIC_KEY=
